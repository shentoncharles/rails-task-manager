<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%= link_to "Delete task",
            tasks_path,
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<p><%=link_to 'Edit', edit_path%></p>
<p><%=link_to 'Back', tasks_path%></p>
